{
  "name": "@degen/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "health": "node -e \"fetch('http://localhost:'+(process.env.PORT||8081)+'/healthz').then(r=>process.exit(r.ok?0:1),()=>process.exit(1))\"",
    "loadtest": "tsx src/loadtest.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx test/lobbies.ts"
  },
  "dependencies": {
    "@degen/shared": "*",
    "@dimforge/rapier3d-compat": "^0.14.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
